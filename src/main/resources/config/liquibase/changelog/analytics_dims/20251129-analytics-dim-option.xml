<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd"
    objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">
    <!-- 005: dim_option materialized view -->
    <changeSet id="005-create-dim-option" author="hamza">
        <sql>
      <![CDATA[
            CREATE MATERIALIZED VIEW analytics.dim_option AS
            SELECT
                row_number() OVER ()::bigint AS dim_option_id,
                o.id AS option_id,
                o.uid AS option_uid,
                o.option_set_id,
                os.uid AS option_set_uid,
                o.code,
                o.name AS name_default,
                TRIM(COALESCE(analytics.translation_value(COALESCE(o.translations, '[]'::jsonb), 'en', 'name'), o.name)) AS name_en,
                TRIM(COALESCE(analytics.translation_value(COALESCE(o.translations, '[]'::jsonb), 'ar', 'name'), o.name)) AS name_ar,
                o.sort_order,
                os.last_modified_by,
                GREATEST(COALESCE(os.created_date, '1970-01-01'::timestamp),
                         COALESCE(os.last_modified_date, '1970-01-01'::timestamp)
                ) AT TIME ZONE 'UTC' AS source_last_modified_at
            FROM public.option_value o
                     LEFT JOIN public.option_set os ON os.id = o.option_set_id;
            ]]>
    </sql>
        <rollback>
            <sql>DROP MATERIALIZED VIEW IF EXISTS analytics.dim_option;</sql>
        </rollback>
    </changeSet>

    <!-- ===== dim_option ===== -->
    <changeSet id="idx-004-dim_option-slm" author="hamza" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <not>
                <indexExists indexName="idx_analytics_dim_option_slm"/>
            </not>
        </preConditions>
        <sql>
      <![CDATA[
            CREATE INDEX CONCURRENTLY idx_analytics_dim_option_slm
                ON analytics.dim_option (source_last_modified_at);
            ]]>
    </sql>
        <rollback>
            <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_analytics_dim_option_slm;</sql>
        </rollback>
    </changeSet>

    <changeSet id="idx-005-dim_option-uid-uniq" author="hamza" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <not>
                <indexExists indexName="idx_analytics_dim_option_uid_uniq"/>
            </not>
        </preConditions>
        <sql>
      <![CDATA[
            CREATE UNIQUE INDEX CONCURRENTLY idx_analytics_dim_option_uid_uniq
                ON analytics.dim_option (option_uid);
            ]]>
    </sql>
        <rollback>
            <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_analytics_dim_option_uid_uniq;</sql>
        </rollback>
    </changeSet>

    <changeSet id="idx-006-dim_option-slm_uid" author="hamza" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <not>
                <indexExists indexName="idx_analytics_dim_option_slm_uid"/>
            </not>
        </preConditions>
        <sql>
      <![CDATA[
            CREATE INDEX CONCURRENTLY idx_analytics_dim_option_slm_uid
                ON analytics.dim_option (source_last_modified_at, option_uid);
            ]]>
    </sql>
        <rollback>
            <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_analytics_dim_option_slm_uid;</sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
