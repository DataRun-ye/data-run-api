<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
       http://www.liquibase.org/xml/ns/dbchangelog
       http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <!-- ChangeSet to create scope_history table -->
    <changeSet id="1-create-scope-history" author="datarun">
        <createTable tableName="scope_history">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"/>
            </column>
            <column name="scope_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="old_data" type="JSONB"/>
            <column name="new_data" type="JSONB"/>
            <column name="changed_at" type="TIMESTAMPTZ" defaultValueComputed="NOW()"/>
            <column name="changed_by" type="UUID"/>
        </createTable>
    </changeSet>

    <!-- ChangeSet to create the trigger function -->
    <changeSet id="2-create-log-scope-change-function" author="datarun">
        <sql>
            CREATE OR REPLACE FUNCTION log_scope_change() RETURNS TRIGGER AS $$
            BEGIN
                IF OLD.scope_data IS DISTINCT FROM NEW.scope_data THEN
                    INSERT INTO scope_history(scope_id, old_data, new_data)
                    VALUES (OLD.id, OLD.scope_data, NEW.scope_data);
            END IF;
            RETURN NEW;
            END;
            $$ LANGUAGE plpgsql;
        </sql>
    </changeSet>

    <!-- ChangeSet to create the trigger -->
    <changeSet id="3-create-scope-audit-trigger" author="datarun">
        <sql>
            CREATE TRIGGER scope_audit
                BEFORE UPDATE ON scope_instance
                FOR EACH ROW EXECUTE FUNCTION log_scope_change();
        </sql>
    </changeSet>

</databaseChangeLog>
